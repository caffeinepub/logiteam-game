module {
  public type SessionId = Nat;
  public type PuzzleId = Nat;
  public type Timestamp = Int; // nanoseconds from Time.now()
  public type UserId = Principal;
  public type TeamCode = Text; // 6-character alphanumeric
};
